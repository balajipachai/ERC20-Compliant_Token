# ERC20-Compliant_Token
Creates a ERC20-Compliant_Token

# Install Dependency
npm install

# It's still in progress, the application has not been developed end-to-end.
# Running the application would result crashing the application.
